% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/three_step.R
\name{vcov.tseLCA_measurement}
\alias{vcov.tseLCA_measurement}
\alias{vcov.tseLCA_covariate}
\alias{vcov.tseLCA_distal}
\alias{vcov.tseLCA_both}
\title{Extract the variance-covariance matrix from a tseLCA model object}
\usage{
\method{vcov}{tseLCA_measurement}(object, ...)

\method{vcov}{tseLCA_covariate}(object, which = c("three_step", "two_step"), ...)

\method{vcov}{tseLCA_distal}(object, ...)

\method{vcov}{tseLCA_both}(
  object,
  which = c("covariate", "distal", "both"),
  step = c("three_step", "two_step"),
  ...
)
}
\arguments{
\item{object}{A \code{tseLCA} object returned by \code{\link[=three_step]{three_step()}}.}

\item{...}{Further arguments (currently unused).}

\item{which}{Character. \code{"three_step"} (default) or \code{"two_step"} for
covariate models; \code{"covariate"}, \code{"distal"}, or \code{"both"} for both models.}

\item{step}{Character. For \code{tseLCA_both}: \code{"three_step"} (default) or
\code{"two_step"}.}
}
\value{
A named square matrix. The two-step vcov is only available when
\code{get.twostep.vcov = TRUE} was set in \code{\link[=three_step]{three_step()}}.
}
\description{
Extract the variance-covariance matrix from a tseLCA model object
}
\examples{
d    <- generate_data(100, "high", "covariate", seed = 1)
fit_m <- three_step(d, paste0("Y", 1:6), n_classes = 3)
vcov(fit_m)   # returns NULL with a message
\donttest{
d   <- generate_data(200, "high", "covariate", seed = 1)
fit <- three_step(d, paste0("Y", 1:6), n_classes = 3,
                  Zp.names = "Zp", use.simple.cov = TRUE)
vcov(fit)   # Q*(T-1) x Q*(T-1) vcov matrix with named rows/cols
}
\donttest{
d   <- generate_data(200, "high", "distal", seed = 2)
fit <- three_step(d, paste0("Y", 1:6), n_classes = 3,
                  Zo.name = "Zo", use.simple.cov = TRUE)
vcov(fit)   # T x T vcov matrix with mu_C1..mu_CT row/col names
}
\donttest{
d   <- generate_data(200, "high", "covariate", seed = 1)
d$Zo <- rnorm(200, mean = c(-1, 0, 1)[d$X], sd = 0.5)
fit <- three_step(d, paste0("Y", 1:6), n_classes = 3,
                  Zp.names = "Zp", Zo.name = "Zo",
                  use.simple.cov = TRUE)
vcov(fit, which = "covariate")
vcov(fit, which = "distal")
}
}
